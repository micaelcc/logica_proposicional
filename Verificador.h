//Assinaturas das funções do Verificador

int is_atom(char);

int is_conector(char);

void remove_spaces(char *);

int par_verify_open_close(char* );

int count_binary_conn(char *);

int count_parentesis(char *);

int count_atoms(char *);

int verify(char *);